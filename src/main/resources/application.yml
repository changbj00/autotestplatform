
spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    primary:
      type: com.alibaba.druid.pool.DruidDataSource
      url: jdbc:mysql://112.126.64.183:3306/test?characterEncoding=UTF-8
      username: root
      password: 12345
      driver-class-name: com.mysql.cj.jdbc.Driver
    local:
      type: com.alibaba.druid.pool.DruidDataSource
      url: jdbc:mysql://127.0.0.1:3306/test?characterEncoding=UTF-8
      username: root
      password: 11111111
      driver-class-name: com.mysql.cj.jdbc.Driver
  mail:
    host: smtp.163.com
    username: 15811419781@163.com
    password: GOIIGZAVUUOMIBJJ
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
  redis:
      # Redis数据库索引（默认为0）
      database: 0
      # Redis服务器地址
      host: 112.126.64.183
      # Redis服务器连接端口
      port: 6379
      pool:
        # 连接池最大连接数（使用负值表示没有限制）
        max-active: 8
        # 连接池最大阻塞等待时间（使用负值表示没有限制）
        max-wait: -1
        # 连接池中的最大空闲连接
        max-idle: 8
        # 连接池中的最小空闲连接
        min-idle: 0
      # 连接超时时间（毫秒）
        timeout: 0
  thymeleaf:
    cache: false
mybatis:
  config-location: classpath:mybatis/mybatis-config.xml
  mapper-locations: classpath:mybatis/mapper/*.xml
  type-aliases-package: com.autotestplatform
logging:
  level:
    com:
      autotestplatform: debug
server:
  port: 8088
